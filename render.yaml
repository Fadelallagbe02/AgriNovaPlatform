services:

  - type: web
    name: agrinova-auth
    runtime: python
    plan: free
    branch: main
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/auth_server.py
    healthCheckPath: /api/health

  - type: web
    name: agrinova-social
    runtime: python
    plan: free
    branch: main
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/social_server.py
    healthCheckPath: /api/health
